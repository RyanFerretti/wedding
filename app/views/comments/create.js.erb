$("#post_header_comment_count_<%= @post.id %>").html("<%= pluralize(@post.comments.count, 'comment') %>");
$("#post_comments_count_<%= @post.id %>").html("<%= pluralize(@post.comments.count, 'comment') %>");
$("#comments_body_inner_<%= @post.id %>").append("<%= escape_javascript(render(:partial => @comment)) %>");
$("#new_comment_<%= h @post.id %>")[0].reset();
$("#addcomment-error-<%= h @post.id %>").hide();        